server:
  port: 8082

spring:
  application:
    name: analyzer
  config:
    import: "configserver:"
    cloud:
      config:
        discovery:
          enabled: true
          serviceId: config-server

logging:
  level:
    ru:
      yandex:
        practicum: DEBUG

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
