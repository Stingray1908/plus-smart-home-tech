server:
  port: 8082

spring:
  application:
    name: analyzer
  config:
    import: configserver:http://localhost:8888

logging:
  level:
    ru:
      yandex:
        practicum: DEBUG

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
